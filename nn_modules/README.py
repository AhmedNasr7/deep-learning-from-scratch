"""
nn-modules: Core layers from scratch.

Each file implements one PyTorch layer using only nn.Parameter and tensor ops.
The goal: understand what nn.Linear, nn.Conv2d, etc. actually do inside.

Planned files:
  linear.py      — Linear (fully connected) layer
  conv.py        — Conv2d (2D convolution)
  batchnorm.py   — BatchNorm1d, BatchNorm2d
  dropout.py     — Dropout, Dropout2d
  pooling.py     — MaxPool2d, AvgPool2d, AdaptiveAvgPool2d
  rnn.py         — RNNCell, LSTMCell, GRUCell
  container.py   — Sequential, ModuleList
"""
