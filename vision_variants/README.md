# Vision Variants

This package provides educational scaffolds for vision transformer variants.

Planned components:
- `swin.py` — Swin Transformer with windowed attention and hierarchical patching
- `hybrid_vit.py` — hybrid CNN-ViT models and patch aggregation
- `train.py` — training script scaffold for variant experiments
