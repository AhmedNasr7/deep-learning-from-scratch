# Video Models

This package contains educational scaffolds for video transformers and
spatiotemporal attention.

Planned components:
- `tubelet_embedding.py` — extract tubelet tokens from video frames
- `video_transformers.py` — TimeSformer / Video ViT / Video Swin building blocks
- `train.py` — training script scaffold for video datasets
